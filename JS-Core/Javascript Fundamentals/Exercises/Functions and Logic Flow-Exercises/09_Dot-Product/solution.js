function solve() {
    //The task is wrong because 2 matrices can only be multiplied if the dimentions of of M1 are m x n and the dimentions of the other (M2) are n x m
    //So i will  not do it (its not that hard actually);
}